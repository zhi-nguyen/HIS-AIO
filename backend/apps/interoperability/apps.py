from django.apps import AppConfig


class InteroperabilityConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.interoperability'
    verbose_name = 'Liên thông Y tế (FHIR / DICOM)'
